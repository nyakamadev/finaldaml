import React from "react";
import "./Hero.css";

const Hero = () => {
  return (
    <section className="hero">
      <div className="hero-overlay">
        <div className="hero-content">
          <button className="hero-cta">the future is ours</button>
        </div>
      </div>
    </section>
  );
};

export default Hero;